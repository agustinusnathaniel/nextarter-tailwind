{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "public/**"],
      "env": []
    },
    "ultracite:check": {
      "outputs": [],
      "inputs": ["biome.json", "src/**"]
    },
    "biome:ci": {
      "outputs": [],
      "inputs": ["biome.json", "src/**"]
    },
    "type:check": {
      "outputs": ["*.tsbuildinfo"]
    }
  }
}
